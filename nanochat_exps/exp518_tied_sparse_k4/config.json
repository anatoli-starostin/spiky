{
  "exp_name": "exp518_tied_sparse_k4",
  "_note": "Fork of exp517. Only the unembedder shape changes: nap 8->6, tph 4096->8192, n_sparse 8->16 -> coverage K=tph*n_sparse/V = 4 (was 1), SAME 8.4M params. Isolated inverse test showed K=1 caps self-decode at 11-78% while K=4 hits 100% at BOTH lr=1e-3 and 1e-2 -> coverage, not LR, is the invertibility lever (corrects the earlier LR diagnosis). Standard LRs/warmup kept (1e-3); the high-LR/no-warmup recipe was only needed for the old fixed-hash row=token head. Tests whether a fully-invertible learnable matmul-free tied head (8.4M vs 12.6M Linear) gives a usable LM vs exp428 1.498.",
  "description": "Tied unembedder, simplified per user idea: use the PROVEN sparse-scatter TinyMHLut (the exp100/body mechanism) to map E->VOCAB, plus an identity/inverse regularizer (token embeddings unembed to themselves). Drops the brittle parts of exp514-516 (custom row=token head, fixed-random hash, assignment STE, high-LR embedder contortion). Unembedder = TinyMHLut(input_dim=E=64, n_heads=1, n_outputs=unemb_n_sparse=8, n_anchor_pairs=8, tph=4096, sparse_scatter_n_outputs=V) with backward_mode=ste (soft costs 57GB on E->V; ste 6.6GB and is the validated sparse-scatter combo) -> 8.4M params (< the 12.58M Linear head), every token covered by ~1 table. Dual stream collapsed to E (predicted embedding in R^E). Losses: MAIN=CE(unemb(pred_emb),next); AUX=CE(unemb(emb_table_subsample),ids) (identity reg). No assignment STE -- the LUT's own ste backward gives gradients. Standard LRs/warmup (unemb & tok_emb 1e-3) since the learnable LUT adapts to the embedder instead of forcing it onto a fixed hash. vs exp428 Linear head 1.498.",
  "context_size": 512,
  "embedding_dim": 64,
  "residual_dim": 64,
  "num_layers": 6,
  "n_heads": 6,
  "d_qk": 64,
  "d_v": 16,
  "rope_base": 10000.0,
  "qkv_input_nap": 6,
  "qkv_tph": 64,
  "qkv_lut_init_std": 0.001,
  "v_input_nap": 6,
  "v_tph": 256,
  "out_input_nap": 6,
  "out_tph": 1024,
  "residual_input_nap": 6,
  "residual_tph": 128,
  "argmax_noise_eps": 0.0,
  "soft_score_temp": 0.5,
  "select_temp": 0.5,
  "soft_learnable_temps": true,
  "soft_use_bf16": true,
  "unemb_nap": 6,
  "unemb_tph": 8192,
  "unemb_n_sparse": 16,
  "unemb_lr": 0.001,
  "tok_emb_lr": 0.001,
  "tok_emb_init": 0.5,
  "aux_ce_weight": 1.0,
  "aux_batch": 8192,
  "random_seed": 42,
  "adam_lr": 0.0003,
  "lut_lr": 0.001,
  "weight_decay": 0.1,
  "mhlut_init_std": 0.001,
  "n_steps": 8000,
  "device_batch_size": 16,
  "total_batch_size": 8192,
  "eval_every": 200,
  "eval_steps": 10,
  "lr_warmup_fraction": 0.1
}
