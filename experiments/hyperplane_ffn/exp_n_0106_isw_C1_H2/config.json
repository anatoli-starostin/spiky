{
  "exp_name": "exp_n_0106_isw_C1_H2",
  "_sweep_note": "inner48 sweep C1 (DECOMPRESS regime, heads check): n_heads 4->2 at V-scale 0.50 (noise 0.472). in48/out48/nap6/tph88. Does fewer routing heads help at width 48?",
  "_arch_note": "exp_n_0085 with ONE change: the attention v_lut's tph 8->88 (nap=6, inner_in=24, inner_out=24, n_heads=4 ALL UNCHANGED). This grows the v_lut ~5x in params via table count only (tables = n_heads*tph*2^nap*inner_out = 4*88*64*24 = 540,672/layer), keeping FLOPs/bandwidth low (still gather-read; tph=88 means 88 gathers/head). Per-layer v_lut 122,880 -> 614,400 (exactly 5.0x). Everything else identical to exp_n_0085: dense q/k Linear, out_proj Linear, FFN slot n_heads=4/inner48/nap7/tph256, untied unembedder, depth 6, n_embd 384, 6 attn heads d=64, seq 512, device_batch 24/grad_accum 2. v_lut emits 384 reshaped to 6 attn heads x 64 for SDPA. Tests whether a 5x-param, low-bandwidth routed V beats the dense Linear V. Targets: exp_n_0084 1.1987 (dense-V), exp_n_0045 1.1977 (tied).",
  "gamma": 0,
  "ffn_type": "compression",
  "tie_unembedder": false,
  "depth": 6,
  "n_embd": 384,
  "n_head": 6,
  "seq_len": 512,
  "_mem_note": "device_batch_size 48->24 (grad_accum 1->2) vs exp_n_0045: the untied head materializes the full [tokens,vocab] logits+grad buffer that the tied graph fused away, which OOM'd at bs48 (LUT dense-K [24576,2048,128]~24GB + logits pushed >79GB). Halving the micro-batch keeps effective batch 24,576 identical (grad accumulation is mathematically equivalent) — science unchanged, memory halved.",
  "device_batch_size": 24,
  "total_batch_size": 24576,
  "n_steps": 3500,
  "lr": 0.0003,
  "weight_decay": 0.1,
  "lr_warmup_fraction": 0.1,
  "eval_every": 200,
  "eval_steps": 10,
  "random_seed": 1,
  "compute_dtype": "bf16",
  "tokenizer_vocab_size": 32768,
  "lut_inner_in_dim": 48,
  "lut_inner_out_dim": 48,
  "lut_n_anchor_pairs": 7,
  "lut_tables_per_head": 256,
  "lut_n_heads": 4,
  "lut_joint_head_compression": false,
  "lut_forward_mode": "hard",
  "lut_use_bf16": false,
  "lut_init_weights_noise": 0.001,
  "lut_base_seed": 1000,
  "lut_learnable_temps": true,
  "v_lut": true,
  "v_lut_inner_in_dim": 48,
  "v_lut_inner_out_dim": 48,
  "v_lut_n_anchor_pairs": 6,
  "v_lut_tables_per_head": 88,
  "v_lut_n_heads": 2,
  "v_lut_forward_mode": "hard",
  "_init_fix_note": "FIX (B) (relaunch): match the v_lut's step-0 V output to the dense Linear V (~0.388 RMS, input-dependent). (1) v_lut_init_weights_noise 0.001->0.363 (~54x the earlier 0.00669) so the table-signal RMS reaches ~0.384 — the earlier ~6.69x was mis-derived (the init output was dominated by the decompress bias, not the table signal). (2) the v_lut's decompress Linear BIAS is zeroed at init (in train.py MinimalGPT init) so V is input-dependent, not a ~0.058 constant. v_lut-specific; the FFN-slot LUT init (lut_init_weights_noise=0.001) is UNCHANGED.",
  "v_lut_init_weights_noise": 0.472
}