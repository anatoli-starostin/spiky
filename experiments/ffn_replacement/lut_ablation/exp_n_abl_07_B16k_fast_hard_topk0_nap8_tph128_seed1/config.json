{
  "exp_name": "exp_n_abl_07_B16k_fast_hard_topk0_nap8_tph128_seed1",
  "_arch_note": "LUT ABLATION ROW 2.1 REPRODUCTION (same config and seeds as the parent; only exp_name and this note differ) so the whole ablation comes from one set of trainers. Not bit-identical to the parent: CUDA training is not deterministic here (no torch.use_deterministic_algorithms; embedding_bag / index_add / scatter_add backwards), the parent trained on an H100 with its own inline-model trainer and the code of 2026-09-03, and this one uses train_fixed.py (model_build; same schedule, optimiser grouping, seeds, data loader, fixed eval). Expected: close to exp_n_0121's 1.180622 (corrected re-score), not to the last digit.",
  "gamma": 0,
  "ffn_type": "compression",
  "tie_unembedder": false,
  "depth": 6,
  "n_embd": 384,
  "n_head": 6,
  "seq_len": 512,
  "_mem_note": "nap9 512-cell soft-backward buffer OOMs at bs24; device_batch 12 / grad_accum 4, effective batch 24576=24576 unchanged.",
  "device_batch_size": 12,
  "total_batch_size": 24576,
  "n_steps": 16000,
  "lr": 0.0003,
  "weight_decay": 0.1,
  "lr_warmup_fraction": 0.1,
  "eval_every": 200,
  "eval_steps": 10,
  "random_seed": 1,
  "compute_dtype": "bf16",
  "tokenizer_vocab_size": 32768,
  "lut_inner_in_dim": 48,
  "lut_inner_out_dim": 48,
  "lut_n_anchor_pairs": 8,
  "lut_tables_per_head": 128,
  "lut_n_heads": 4,
  "lut_joint_head_compression": false,
  "lut_forward_mode": "hard",
  "lut_use_bf16": false,
  "lut_init_weights_noise": 0.001,
  "lut_base_seed": 1000,
  "lut_learnable_temps": true,
  "_sweep_tag": "nap8-tph128"
}
