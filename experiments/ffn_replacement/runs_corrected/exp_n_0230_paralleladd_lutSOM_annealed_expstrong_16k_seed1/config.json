{
  "exp_name": "exp_n_0230_paralleladd_lutSOM_annealed_expstrong_16k_seed1",
  "_arch_note": "LUT Kohonen/SOM annealed topographic-smoothness arm (task 315b7693). Base = exp_n_0221 parallel-add (narrowed FFN + reduced LUT). Penalty = lambda*mean_pairs exp(-d_H^2/(2 sigma^2))*||v_c-v_c'||^2 over cell pairs (d_H=Hamming dist of addresses), sigma annealed 3.0->0.5 (exp) over training progress; lambda=10.0. Graded generalization of the flat Hamming-1 TV (sigma->0 limit). Guarded (all knobs absent -> byte-identical, verified). device_batch 48/ga1 (eff 24576), 16k schedule (warmup 1600, cosine to 0.1x floor, peak 3e-4, wd 0.1, seed 1, corrected eval). Raw som penalty + current sigma logged each eval (lut_som, lut_som_sigma). Calibration: raw som@trained-16k(sigma=2)~0.013, loss~3.67 -> lambda*som ~{1e-3,1e-2,1e-1}*loss at lambda~{0.28,2.8,28}. Queued after the TV arms 0225-0228.",
  "gamma": 0,
  "ffn_type": "compression",
  "hybrid_add": true,
  "dense_activation": "gelu",
  "dense_inner_dim": 384,
  "tie_unembedder": false,
  "depth": 6,
  "n_embd": 384,
  "n_head": 6,
  "seq_len": 512,
  "device_batch_size": 48,
  "total_batch_size": 24576,
  "n_steps": 16000,
  "lr": 0.0003,
  "weight_decay": 0.1,
  "lr_warmup_fraction": 0.1,
  "eval_every": 500,
  "eval_steps": 10,
  "random_seed": 1,
  "compute_dtype": "bf16",
  "tokenizer_vocab_size": 32768,
  "lut_inner_in_dim": 48,
  "lut_inner_out_dim": 48,
  "lut_n_anchor_pairs": 8,
  "lut_tables_per_head": 128,
  "lut_n_heads": 4,
  "lut_joint_head_compression": false,
  "lut_forward_mode": "hard",
  "lut_use_bf16": false,
  "lut_init_weights_noise": 0.001,
  "lut_base_seed": 1000,
  "lut_learnable_temps": true,
  "lut_impl": "light",
  "lut_forward_confidence": true,
  "lut_confidence_form": "margin",
  "lut_tables_no_decay": true,
  "lut_z_norm": false,
  "lut_read_top_n": 1,
  "lut_read_tau": 0.5,
  "lut_read_tau_learnable": true,
  "lut_som_lambda": 10.0,
  "lut_som_sigma0": 3.0,
  "lut_som_sigma_final": 0.5,
  "lut_som_anneal": "exp"
}