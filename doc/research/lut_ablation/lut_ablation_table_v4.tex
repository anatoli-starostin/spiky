% LUT core-module ablation table (documentation only; no experiments were run to build it).
% Build: latexmk -pdf -interaction=nonstopmode -halt-on-error lut_ablation_table_v4.tex
\documentclass[10pt]{article}
\usepackage[landscape,margin=0.55in]{geometry}
\usepackage{lmodern}
\usepackage[T1]{fontenc}
\usepackage{amsmath,amssymb}
\usepackage{booktabs,longtable,array}
\usepackage{xcolor}
\usepackage[hang,flushmargin]{footmisc}
\usepackage{hyperref}
\hypersetup{colorlinks=true,linkcolor=black,urlcolor=blue}

\newcommand{\tbd}[1]{\textcolor{red!70!black}{\textbf{TBD}~--- #1}}
% \code{...}: typewriter, with a line-break opportunity after every underscore (long class names)
\newcommand{\code}[1]{{\renewcommand{\_}{\textunderscore\allowbreak}\texttt{#1}}}
\newcommand{\sgn}{\operatorname{sgn}}
\newcommand{\note}[1]{\textsuperscript{\textbf{#1}}}
\renewcommand{\arraystretch}{1.25}
\setlength{\LTpre}{4pt}\setlength{\LTpost}{4pt}

\title{LUT core-module ablation: three generations of lookup math\\[2pt]
\large Comparison table}
\date{2026-09-13}
\author{}

\begin{document}
\maketitle
\vspace{-2.5em}

\section*{Protocol}
\textbf{Architecture:} LUT transformer, $E=384$, 6 layers, 6 attention heads. FFN = \code{CompressionMultiHeadLUT} with $H=4$ heads $\times$ $tph=128$ tables
($P=H\cdot tph=512$ tables per token), $d_{in}=d_{out}=48$, $n=8$ anchor pairs per table ($K=2^n=256$ cells); \code{compress} $384\to192$, \code{decompress} $192\to384$.
\textbf{Training:} 16{,}000 steps, batch size 48 (24576 tokens), untied embeddings, LUT tables without weight decay.
\textbf{Eval:} \code{evaluate\_bpb\_fixed}, 48 rows $\times$ 100 steps, first 12 rows skipped.

\medskip
\noindent\fbox{\parbox{\dimexpr\linewidth-2\fboxsep-2\fboxrule\relax}{%
\textbf{One seed per row.} Every validation bpb below is a single run, and there are \textbf{zero seed replicates} at this geometry.
The only LUT seed-noise figure on record is a 4K three-seed sd of 0.009642, a \emph{lower bound}; ``units'' below are multiples of it (the vanilla two-seed range at 16K is 0.00335).
\textbf{Every Gen-3 difference in Table B is inside one unit}: 3.2 vs 3.1 $-$0.000016 ($<$\,0.01), 3.3 vs 3.2 $-$0.0056 (0.6).}}

\section*{Counting convention}
\begin{itemize}\setlength\itemsep{0pt}
\item Core LUT only, per token, one layer. The \code{compress}/\code{decompress} projections are identical in every row and excluded (147,456 MACs forward).
\item \textbf{FLOP} = one scalar float operation ($+,-,\times,\div$, a comparison, $|\cdot|$, $\exp$, $\log\sigma$, $\sigma$). \textbf{MAC} = a multiply accumulated into a sum; 1 MAC = 2 FLOPs,
  included in the FLOP column. Memory reads and integer work are not counted.
\item \textbf{Forward} = inference. \textbf{Backward} = all additional training arithmetic, including per-table gradients of per-layer scalars and training-only regularisers
  (amortised over the 24,576 tokens of a step).
\end{itemize}

\newpage
\section*{Table A --- forward function and gradient function}
Per variant, (a) is the function computed on the forward pass and (b) the function whose gradient training uses: the weights always receive the gradient of (a),
while the input, the temperatures and the learned per-layer scalars receive the gradient of (b) with the table weights held constant.
\footnotesize
\begin{longtable}{>{\raggedright\arraybackslash}p{2.9cm} >{\raggedright\arraybackslash}p{10.2cm} >{\raggedright\arraybackslash}p{10.6cm}}
\toprule
\textbf{Variant} & \textbf{(a) Forward: the function computed} & \textbf{(b) Backward: the function whose gradient is used} \\
\midrule
\endfirsthead
\toprule
\textbf{Variant} & \textbf{(a) Forward: the function computed} & \textbf{(b) Backward: the function whose gradient is used} \\
\midrule
\endhead
\midrule \multicolumn{3}{r}{\emph{continued on next page}} \\
\endfoot
\bottomrule
\endlastfoot

\multicolumn{3}{l}{\textbf{GEN 1 --- Spiking Manifesto basic model}} \\[2pt]
1.1 Hard forward, two-alternatives backward &
$y=\sum_tW_t[c_t]$, summed over tables $t$: each of table $t$'s $n$ anchor pairs gives a signed margin $u_j=z[a_j]-z[b_j]$; the $n$ signs $[u_j>0]$ are the bits of the binary index $c_t\in\{0,\dots,2^n-1\}$ of the row read from $W_t\in\mathbb R^{K\times d}$, $K=2^n$. &
The 2-cell blend $\sum_t\big[(1-U_t)W_t[c_t]+U_tW_t[c_t']\big]$, $U_t=U(u_{j^\star})$, $U(u)=0.5/(1+|u|)$, with the least-confident pair $j^\star=\arg\min_j|u_j|$ and $c'$ ($c$ with bit $j^\star$ flipped) held fixed.
The pairing with (a) is exact as a stop-gradient composite; the blend is $C^1$ across a bit flip and jumps only where $j^\star$ switches. \\[3pt]
1.2 Soft forward, two-alternatives backward &
\multicolumn{2}{p{\dimexpr 20.8cm+2\tabcolsep\relax}}{\textbf{(a) = (b):} the blend of 1.1(b), applied at eval too.} \\[4pt]
\midrule
\multicolumn{3}{l}{\textbf{GEN 2 --- FastMHL math}} \\[2pt]
2.1 Hard forward, all-alternatives backward (all $K$ cells) &
$y=\sum_tW_t[c_t]$, as 1.1(a). &
The $K$-cell softmax sum $\sum_t\sum_{\ell<K}\mathrm{softmax}_\ell(s_\ell/T_{sel})\,W_t[\ell]$, $s_\ell=\sum_j\pm u_j/(T_s+|u_j|)$ ($+$ where bit $j$ of $\ell$ is 1), temperatures $T_s,T_{sel}$.
A globally $C^1$ function of the input, independent of the address; it reads all $K$ rows. \\[3pt]
2.2 Hybrid\_smooth two-alternatives forward, all-alternatives backward &
$y=\sum_t\big[(1-w_t)W_t[c_t]+w_tW_t[c_t']\big]$, $w_t=\sigma(-2\rho_{j^\star}/T_{sel})$, $\rho_j=|u_j|/(T_s+|u_j|)$, $\sigma$ the logistic sigmoid; applied at eval too. &
The $K$-cell softmax sum of 2.1(b). \\[3pt]
2.3 Hard forward, 2-alternatives backward &
$y=\sum_t W_t[c_t]$ &
\tbd{not implemented; open decision.} \code{FastMultiHeadLut} raises \code{ValueError} for \code{backward\_topk>0} unless \code{forward\_mode='hybrid\_smooth'}. \\[3pt]
2.4 Hybrid\_smooth two-alternatives forward, 2-alternatives backward &
\multicolumn{2}{p{\dimexpr 20.8cm+2\tabcolsep\relax}}{\textbf{(a) = (b):} the 2-cell blend of 2.2(a) (\code{backward\_topk=1}: it counts cells beyond the addressed one, so 2 alternatives is 1, not 2).} \\[4pt]
\midrule
\multicolumn{3}{l}{\textbf{GEN 3 --- LightMHL math (LookupFFN-inspired): learned margin score}} \\[2pt]
3.1 Learned margin score, per-layer $(g,\beta,\gamma)$ &
\multicolumn{2}{p{\dimexpr 20.8cm+2\tabcolsep\relax}}{\textbf{(a) = (b):} $y=\sum_t s_tW_t[c_t]$ with the address $c_t$ held fixed; $s_t=e^{g}\,S_t\,P_t^{\gamma}$, $S_t=\sum_jm_j$, $P_t=\prod_j\sigma(\beta m_j)$, $m_j=|u_j|$, computed as $S_t\exp\!\big(g+\gamma\sum_j\log\sigma(\beta m_j)\big)$;
one $(g,\log\beta,\log\gamma)$ per layer, initialised at $(0,2,1)$. Discontinuous at cell boundaries, since $\sigma(0)>0$ (trained jump: median 2.19\% of $\|y\|$).} \\[3pt]
\textbf{3.2 The Gen-3 form:} learned margin score, per-layer $(\beta,\gamma)$ &
\multicolumn{2}{p{\dimexpr 20.8cm+2\tabcolsep\relax}}{\textbf{(a) = (b):} 3.1 with $g$ fixed at 0, $s_t=S_tP_t^{\gamma}$ (jump 2.22\%).} \\[3pt]
3.3 $=$ 3.2 $+$ Hamming-1 cell TV, $\lambda=10$ &
\multicolumn{2}{p{\dimexpr 20.8cm+2\tabcolsep\relax}}{\textbf{(a) = (b):} 3.2 (jump 1.78\%); training adds $\lambda\cdot\frac16\sum_{\text{layers}}\frac{1}{P\,n\,2^{n-1}}\sum_t\sum_{(c,c')}\|W_t[c]-W_t[c']\|^2$, over cell pairs differing in one bit, to the loss.} \\
\end{longtable}
\normalsize

\section*{Table B --- cost and results}
Per token, one layer, core LUT only; $P=512$, $n=8$, $K=256$, $d=48$. Validation bpb: \textbf{one seed per row}.
\footnotesize
\setlength{\tabcolsep}{3pt}% seven columns: narrower gutters keep the table inside the text block
\begin{longtable}{>{\raggedright\arraybackslash}p{1.3cm} >{\raggedright\arraybackslash}p{2.8cm} >{\raggedright\arraybackslash}p{4.2cm} >{\raggedright\arraybackslash}p{2.0cm} >{\raggedright\arraybackslash}p{2.8cm} >{\raggedright\arraybackslash}p{4.3cm} >{\raggedright\arraybackslash}p{6.0cm}}
\toprule
\textbf{Variant} & \textbf{Forward FLOPs} & \textbf{Backward FLOPs} & \textbf{Forward MACs} & \textbf{Backward MACs} & \textbf{Validation bpb} & \textbf{Implementation (class used)} \\
\midrule
\endfirsthead
\toprule
\textbf{Variant} & \textbf{Forward FLOPs} & \textbf{Backward FLOPs} & \textbf{Forward MACs} & \textbf{Backward MACs} & \textbf{Validation bpb} & \textbf{Implementation (class used)} \\
\midrule
\endhead
\midrule \multicolumn{7}{r}{\emph{continued on next page}} \\
\endfoot
\bottomrule
\endlastfoot
1.1 &
$P(2n+d)=\mathbf{32{,}768}$ &
$P(5d+2n+9)=\mathbf{135{,}680}$ &
$0$ &
$P\cdot2d=\mathbf{49{,}152}$ &
\tbd{not yet implemented} &
\code{MultiHeadLut(smooth\_mode=False, n\_alternatives=1, uncertainty\_mode=INVERSE\_L1)}; not wired into \code{CompressionMultiHeadLUT}. \\
1.2 &
$P(4d+4n+4)=\mathbf{116{,}736}$ &
$P(8d+9)=\mathbf{201{,}216}$ &
$P\cdot2d=\mathbf{49{,}152}$ &
$P\cdot4d=\mathbf{98{,}304}$ &
\tbd{not yet implemented} &
\code{MultiHeadLut(smooth\_mode=True, n\_alternatives=1)}; not wired in. \\
\midrule
2.1 &
$P(2n+d)=\mathbf{32{,}768}$ &
$P(2Kd+4nK+9K+11n+d)$ $=\mathbf{18{,}026{,}496}$\note{e} &
$0$ &
$P(Kd+2nK)$ $=\mathbf{8{,}388{,}608}$ &
\textbf{1.180622} (\code{exp\_n\_0121})\note{f} &
\code{FastMultiHeadLut(forward\_mode='hard', backward\_topk=0)}. \\
2.2 &
$P(2kd+4n+5)=\mathbf{117{,}248}$, $k=2$ cells read &
$P(2Kd+4nK+9K+11n+2kd)$ $=\mathbf{18{,}100{,}224}$\note{e} &
$P\,kd=\mathbf{49{,}152}$ &
$P(Kd+2nK+kd)$ $=\mathbf{8{,}437{,}760}$ &
\tbd{not yet run} &
\code{FastMultiHeadLut(forward\_mode='hybrid\_smooth', backward\_topk=0)}. \\
2.3 &
$P(2n+d)=32{,}768$ &
n/a &
$0$ &
n/a &
\tbd{not implemented; open decision} &
Would be \code{FastMultiHeadLut(forward\_mode='hard', backward\_topk=1)}, which the code rejects. \\
2.4 &
$P(2kd+4n+5)=\mathbf{117{,}248}$ &
$P(4kd+9n+15)=\mathbf{241{,}152}$ &
$P\,kd=\mathbf{49{,}152}$ &
$P\,2kd=\mathbf{98{,}304}$ &
\tbd{not yet run} &
\code{FastMultiHeadLut(forward\_mode='hybrid\_smooth', backward\_topk=1)}. \\
\midrule
3.1 &
$P(2kd+7n+2)=\mathbf{78{,}848}$, $k=1$\note{g} &
$P(4kd+10n+10)=\mathbf{144{,}384}$\note{g} &
$P\,kd=\mathbf{24{,}576}$ &
$P\,2kd=\mathbf{49{,}152}$ &
\textbf{1.169344} (\code{exp\_g\_0247}) &
\code{LightMultiHeadLUT(confidence\_form='learned\_margin', learned\_margin\_init=(0, 2, 1))}. \\
\textbf{3.2} &
$P(2kd+7n+2)=\mathbf{78{,}848}$ &
$P(4kd+10n+8)=\mathbf{143{,}360}$\note{g} &
$\mathbf{24{,}576}$ &
$\mathbf{49{,}152}$ &
\textbf{1.169328} (\code{exp\_g\_0248}) &
As 3.1 with \code{learned\_margin\_freeze\_g=True}. \\
3.3 &
$\mathbf{78{,}848}$ (as 3.2) &
$\mathbf{149{,}504}$ (3.2 $+$ TV)\note{h} &
$\mathbf{24{,}576}$ &
$\mathbf{51{,}200}$\note{h} &
\textbf{1.163698} (\code{exp\_g\_0249}) &
As 3.2, plus \code{(10 * model.lut\_tv\_penalty()).backward()} per step (\code{lut\_cell\_smoothness: 10.0}). \\
\end{longtable}
\normalsize
\noindent\textbf{Notes to Table B.}
\begin{itemize}\setlength\itemsep{0pt}\footnotesize
\item[\note{e}] Dominated by $2Kd$: every table's $K$ rows are read; several \code{[tokens, 512, 256]} fp32 buffers of about 12.9\,GiB at 24,576 tokens.
\item[\note{f}] Re-score of the saved run under the corrected eval (0 missing keys); its in-run batch-coupled number was 1.191455.
\item[\note{g}] Per table, forward: $n$ comparisons, $n$ differences, $n$ absolute values, $n$ products $\beta m_j$, $n$ $\log\sigma$, the two sums, $\times\gamma$, $+g$, $\exp$ and the final product ($7n+2$), plus the scaled row ($2kd$).
  Backward: the row terms ($4kd$), the input term ($8n+3$) and the per-table accumulation of the parameter gradients ($\partial g$ 2, $\partial\log\gamma$ 3, $\partial\log\beta$ $2n+2$); 3.2 drops $\partial g$.
  The parameter gradients are scalar multiplies, so they add no MACs.
\item[\note{h}] Training only, once per step: $P\,n\,2^{n-1}=524{,}288$ cell pairs per layer, $3d$ FLOPs and $d$ MACs per pair in each direction (150,994,944 FLOPs, 50,331,648 MACs per layer per step),
  i.e. $+6{,}144$ FLOPs and $+2{,}048$ MACs per token over 24,576 tokens. Inference cost is 3.2's.
\end{itemize}

\section*{The Gen-3 form (one seed each)}
\begin{itemize}\setlength\itemsep{1pt}
\item \textbf{Learned margin score with per-layer trainable $\beta$ and $\gamma$ (3.2). Drop $g$:} freezing it leaves bpb ($-$0.000016 vs 3.1) and the $\gamma$ profile (max $|\Delta\gamma|$ 0.016) unchanged.
  Trained $\gamma$ 1.04--1.60, highest mid-depth; $\beta$ 1.76--2.17 and nearly irrelevant to the score's shape. Both were still drifting at 16K.
\item \textbf{Cell TV (3.3): $-$0.0056 vs 3.2, a mild regularisation benefit}, not established (0.6 units). \textbf{The mechanism is mostly table shrinkage, not smoothing:}
  table L2 norms fell to 0.22--0.35$\times$ of 3.2's, while the scale-free ratio $\overline{\|W[c]-W[c']\|^2}/\overline{\|W[c]\|^2}$ (2.0 for independent random cells)
  moved only from 1.74--1.95 to 1.34--1.57 across layers.
\end{itemize}

\section*{Where the code differs from the original brief}
\begin{itemize}\setlength\itemsep{0pt}
\item 1.1's input gradient is a surrogate (the derivative of 1.2's blend); its weights update only the addressed row.
\item The status report's $u(\delta)=0.5/(T+|\delta|)$ has a temperature; the code's $0.5/(1+|u|)$ has none (\code{l\_projection.py:96}, \code{lutorch.cu:797}).
\item Gen 2's hybrid\_smooth weight $\sigma(-2\rho_{j^\star}/T_{sel})$ is a temperature-controlled soft-sign, not Gen 1's $U(u)$.
\end{itemize}

\section*{Remaining work}
\begin{itemize}\setlength\itemsep{0pt}
\item \textbf{Run 2.2 and 2.4:} fork \code{exp\_n\_0121} verbatim, changing only \code{lut\_forward\_mode} / \code{lut\_backward\_topk}. No matching run exists (earlier hybrid\_smooth runs are $n=6$, tied, old eval;
  \code{exp\_n\_0188} is $n=7$ with a gate on).
\item \textbf{Implement 1.1, 1.2:} wire Gen 1 into \code{CompressionMultiHeadLUT}, which accepts only \code{lut\_impl} $\in\{$\code{fast}, \code{light}, \code{bh4}$\}$ (\code{compression\_mhl.py:190}), as \code{MultiHeadLut} with \code{anchor\_candidates} [128, 4, 48].
  \textbf{Open decision, 2.3:} patch \code{FastMultiHeadLut} (\code{fast\_multi\_head\_lut.py:1417}) or drop the row. A hard-forward Gen-3 variant has not been designed.
\item \textbf{Seeds:} zero replicates at this geometry; one extra seed of \code{exp\_g\_0248} ($\approx$\,0.95\,h) would give a real LUT seed spread.
  Reference: vanilla dense FFN 1.1651 / 1.1618 at 16K (two seeds), 1.1154 at 48K.
\end{itemize}
\end{document}
